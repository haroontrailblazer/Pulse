# Verification — 13 September 2026

- Navigation and the back gesture, all three surfaces: before this change nothing in the product touched window.history, so `page` was React state and a back press left the app from anywhere. Measured against the unchanged build at 1440, 390 and 360 px: four in-app navigations produced zero history entries (history.length stayed 2), the URL never changed at any of the six destinations, one back press after three taps landed on about:blank, a reload returned to Overview, and a back press with the More sheet, the Help sheet or a provider sheet open left the dashboard entirely -- six overlay cases out of six. Now every destination is a history entry with its own URL and every dismissible overlay is a history entry with no URL, so one press closes a sheet or changes a place and never both. Routes live in shared/navigation.js; the base is read from the document because the hosted site answers under /app while the EXE, the APK and the dev server answer at /. Policy is linear: Overview, Watchlist, then the Overview icon leaves three entries and takes three presses to unwind; re-tapping the destination you are on adds none. Filters, search, the developer tool selection and the map region and zoom remain controls on a page and earn no entry, and the security utilities' token and hash inputs are never routable.
- Per-surface specialisation: the website gains shareable reload-safe URLs and a working forward button, and its first entry is written with replaceState so back always returns the reader to whatever preceded the dashboard -- nothing traps them. The EXE binds Alt+Left and Alt+Right in the renderer, where a focused text field can be seen and left alone, plus the mouse's fourth and fifth buttons through app-command in the main process, and is the only surface that draws a back control because it is the only one with neither browser chrome nor a system gesture. The APK registers one OnBackPressedCallback; at the root it is disabled so the platform plays its own predictive back-to-home preview and the task is backgrounded rather than finished, and below API 34, where no preview exists, the press is confirmed with a toast instead.
- The APK mechanism was corrected by device measurement, not reasoning. The obvious implementation reads WebView.canGoBack() natively; on the API 36 emulator that reported false at a moment when the page reported history.length 2, because same-document pushState entries are not in the WebBackForwardList it consults. Trusting it would have closed the app with a sheet still open. The web layer now reports whether Pulse has anywhere to go whenever that changes, the callback is armed from that alone, and the press is handed back to the web layer to act on. Chromium's session-history cap was also measured rather than assumed: exactly 50 entries, oldest pruned first, so after 49 in-app steps the arrival entry is evicted. No depth cap was added, because capping would stop back retracing and this is what Chrome does on any website.
- Gates. `npm run qa:nav` drives real back and forward gestures across four profiles (dev at /, hosted at /app, EXE-shaped with the preload bridge defined, APK-shaped at 360x640 with the platform pinned) over twelve assertion groups: every place reachable and round-tripping through its URL, the reader's own reported sequence retraced press by press, every overlay taking an entry and closing without changing the place, the boundary, forward, reload, cold deep links, a dead-press detector, the one place sheets nest, single-top re-tapping, scroll restoration, and the EXE chevron and keyboard shortcuts. The same gate reports PROBLEMS against the unchanged build, which is how it is known to measure anything: 32 problems across 8 of 11 groups before, 0 after.
- `npm run qa:apk-nav` drives the hardware back key on a connected device over the WebView's own devtools socket, because Playwright cannot attach to a WebView target. 22 checks pass on the API 36 emulator: the real back key retraces instead of closing Pulse, a sheet closes without leaving the page, the root press leaves the foreground while keeping the process warm, a warm relaunch resumes on the same place with a working back, and a back press with the soft keyboard up closes the keyboard rather than the sheet and keeps what was typed. The below-API-34 confirmation path is covered only by PulseBackTest on the JVM; this emulator is API 36.
- Regression checks: 118 Node tests pass (13 new, covering route round-trips at both bases, the dot-free slug rule both extension-less fallbacks depend on, base detection including /dashboard.html, the frozen ?watchlist entry point, that exactly one module touches window.history, the Android wiring read with comments stripped so it cannot pass on prose, and that no overlay kind is routable). Five Android JVM tests cover the arm-and-leave decision. `npm run qa:ui` reports zero tiny text, zero low-contrast pairs, zero small targets and zero overflow across all widths and both themes; `npm run qa:responsive` passes at 390, 1080 portrait and 1440. The EXE chevron is gated on the preload bridge, which Playwright never defines, so no existing geometry number or captured screenshot changes. Verified in the real Electron app over CDP: the chevron is dimmed at the root and live once there is history, /incidents is served by the bundled server, Alt+Left goes back, Alt+Right goes forward, and two further presses at the root leave the window open.
- Two defects found and fixed while the page was open. The navigation sidebar is class-toggled rather than unmounted, so a drag interrupted by a back press or a resize left translateY(...) on a live element and the sheet would open off-screen next time; its inline transform is now cleared whenever the sheet closes. And the incident inbox was simply lost when a provider row was tapped, because the sheet was a single slot; it is now a small stack, so back and the close button both return the reader to the inbox they were reading.
- Not done, and deliberately: no robots.txt or noindex change, because nobody asked for an SEO posture change and vite copies public/ into the APK and the asar, which would move the build fingerprint. No per-provider deep links, because a routable provider sheet is the one thing this model refuses and the notification PendingIntent request codes are shared. Separately found and not changed: desktop/main.cjs binds no back gesture at all today, so "the exe directly comes out from the app" cannot have been a back press -- win.on("close") only preventDefaults while background alerts are enabled, so Ctrl+W or Alt+F4 with alerts off destroys the window and window-all-closed quits the app. That is a different defect in a different file and is reported rather than silently changed.
- Background and navigation update: foreground checks now run every two minutes and hidden renderers unsubscribe and stop timers. Native watchlist alerts, Windows close-to-tray, and a resizable Android RemoteViews widget are implemented. Thirty-one Node tests and five native Java tests pass. APK signature/zip alignment, receiver/WorkManager/notification manifest entries, provider catalog, and matching web assets were checked. Current Windows ASAR contains matching background modules and all 12 web/brand assets. The request-budget model is documented in design/background-monitoring.md; no physical battery measurement was made. Android launcher, notification, Doze and reboot interactions remain untested because no device/emulator is connected.
- Interface update: Phosphor replaces Lucide; OpenAI has a monochrome Blossom vector, with restored Anthropic, AWS, Azure and Twilio marks. Developer tools is a dedicated page. Map, incidents, dependencies and watchlist have no duplicate top console/overall summary. Watchlist totals and filters are scoped correctly. Desktop light and 390 px light/dark layouts were reviewed with no horizontal overflow.

- Production web build: passed.
- Live map integration: the map now consumes the same live provider snapshots and clock as the dashboard. Eight new tests cover regional worst-state aggregation, provider-wide incidents without invented locations, stale/unknown exclusion, word-boundary and accent matching, maintenance/partial unknown data, recovery, resolved incidents, and deduplicated provider counts. Current feeds populated named regional components; provider incident drill-down, severity filtering, and My stack filtering passed. A separately labeled synthetic browser fixture verified red Tokyo and amber London pins, keyboard selection, dark colors, removal of all colored pins after expiry, and green recovery with cleared issue counts. The fixture is ignored development data and is not part of the production app. Live desktop and 390 px phone views checked in both themes with no horizontal overflow or browser console errors; source evidence and developer workflow details remain readable in the scrollable insight panel.
- Custom logo: rounded P monogram and separate signal dot replace the generic activity glyph in the brand mark, footer, favicon, and Android app listing. Existing Manrope wordmark and theme colors retained. SVG geometry is shared between React and the asset generator. Light/dark browser appearance and 16/24/32/48/64 px icon artwork inspected. Delivered transparent black/white SVG and 1,024 px PNG marks, app icon, and a presentation sheet. Android launcher foregrounds verified at 108 dp in all five densities, with charcoal adaptive background and separate circular legacy assets. Windows resource editing is now enabled with code signing disabled; all seven icon sizes embedded in both the application and portable launcher match the generated ICO. All 12 web and brand assets in APK and Windows ASAR match dist by SHA-256. Browser console reported no errors.
- Sidebar overflow fix: reproduced 1,020 px of content in a 650 px fixed sidebar with no scrolling. Navigation, watched services, and the download card now scroll in a bounded flex child; Help, Settings, and the workspace footer stay visible. Verified 1,366 x 650 desktop in both themes, 390 x 667 phone, and 740 x 360 landscape with no sidebar or page horizontal overflow. Checked download and Settings dialogs, keyboard scrolling, mobile initial focus, Tab/Shift+Tab wrapping, Escape and focus return, body scroll-lock cleanup, and resizing an open drawer to desktop. Added two temporary watched services to verify the six-service View all shortcut, then restored the original watchlist. Browser console reported no errors. All 20 existing tests pass.
- Reference palette update: sampled both themes at https://opencode.ai/data/. Light uses white `#FFFFFF`, text `#161616`, secondary `#5C5C5C`, and muted `#707070`; dark uses charcoal `#161616`, white text, secondary `#D4D4D4`, and muted `#A3A3A3`. Controls use monochrome accents; atlas locations now use semantic live-status colors. Fonts remain DM Sans and Manrope. A normalized before/after comparison confirmed only color values changed in theme CSS and App.jsx; typography, spacing, selectors, and behavior are preserved. Favicon and native icon colors regenerated. Desktop (1,440 px) and phone (390 px) layouts checked in both modes with no horizontal overflow. Six sampled light text/surface pairs measured 4.95:1–18.10:1; six dark pairs measured 6.76:1–17.34:1. These are focused checks, not a full accessibility audit.
- Node test suite: all 44 tests passed. Coverage includes monitoring and SSE, package validation, OSV pagination/deduplication/withdrawn advisories/failures, JWT Unicode and invalid claims, SHA-256 reference vectors, and component region labels.
- Developer expansion: all nine new public feeds returned normalized live readings (npm, PyPI & Python, Docker, RubyGems, CircleCI, Postman, Sentry, Snyk, HackerOne). Catalog now contains 28 providers across seven categories. Python status intentionally includes wider Python infrastructure; Docker includes its other developer services.
- Security workbench: browser queries returned 5 advisory records for npm lodash 4.17.20 and 10 for PyPI requests 2.19.0. These are observed samples and can change; IDs can share CVE aliases. Exact-version validation, local JWT decoding/expiry/clear action, SHA-256 text hashing, and matching-checksum comparison passed. Both themes and the 390 px layout checked without page overflow. Source API limits, local-only inputs, and signature-verification limitations are explained in the interface and README.
- Live monitoring: two successive SSE snapshots verified automatic refresh and advancing timestamps. The second snapshot at 07:33:56 UTC returned 16 verified providers out of 19, 1,474 components, and 7 active incidents. The first snapshot included an Atlassian timeout that recovered on the next sweep. These counts are a verification sample, not fixed application data.
- Developer console: verified component search, filtering to non-operational services, watchlist impact, observed changes, feed diagnostics, and copying a provider curl command. Desktop and 390 px phone layouts checked in light/dark themes with no horizontal overflow. A fresh browser session reported no console errors.
- Windows portable executable: built successfully at `releases/Pulse-1.0.0-Windows.exe` (104,038,286 bytes). The executable is unsigned. Its embedded JavaScript matches the current production build by SHA-256.
- Windows SHA-256: `E39793EF049D5ED8F051D29F775C7B8B7A7B23353F5EB323BD5C2A1315AE00B5`.
- Packaged Windows runtime smoke check: the bundled server served the dashboard and returned all 28 providers, including all nine additions, with 25 fresh feeds in the latest check. An isolated Electron run tested preload/IPC, persisted preferences, closing to tray, disabling monitoring and clean exit. Native toast delivery still requires an OS notification check.
- Browser checks: desktop layout, 390 px phone layout with no horizontal overflow, provider search, category filtering, provider details, watchlist editing and persistence after reload, mobile navigation, industry filtering, map region and hub selection, and status brief export passed.
- Atlas redesign: precomputed coastlines, country boundaries, and geographic grid replace the dotted map. Region selection reframes the map, extra zoom focuses on the selected hub, and reset restores the world view. Map markers, location buttons, keyboard selection, Europe/Asia Pacific/North America views, and expanded mode checked. Phone controls measure 44 x 44 px. Coordinates and selected-location details remain readable below the map; no geographic calculations run in the browser. Geometry is rounded to 0.1 SVG units during generation to avoid runtime geographic calculations. The current bundle triggers Vite's advisory size warning.
- Production dependency audit: 0 vulnerabilities. Full development-tool audit: 3 moderate advisories in the Capacitor CLI's Xcode/UUID dependency chain; these are not shipped as production dependencies.
- Android APK: built successfully with Microsoft OpenJDK 21.0.12.1, Gradle 8.14.3, Android SDK platform 36 revision 2, and Build Tools 36.0.0. The user approved Google SDK license acceptance. Tool archives were verified against publisher checksums before installation.
- Android artifact: `releases/Pulse-1.0.0-Android.apk` (8,139,828 bytes), package `app.pulse.status`, version `1.0.0` / code `1`, minimum API 24 (Android 7.0), target/compile API 36. This is a debug-signed development APK, not a production store release.
- Android package checks: APK Signature Scheme v2 verification passed; zip alignment check passed; manifest, DEX, Capacitor configuration, and plugin registry are present. All five HTML, CSS, and JavaScript assets embedded in both the APK and Windows ASAR exactly match the current production web build by SHA-256. Android device/emulator runtime interactions have not been tested.
- Android SHA-256: `9406135C56D69B9BE94FFC7944AAA541ABF9884470D82085219C2B0D3D1F7909`.
- Repeat Android builds with `npm.cmd run build:android`. Java and SDK are installed locally under `.cache/android-toolchain`; Windows Gradle cache is under `%TEMP%/pulse-gradle`, avoiding a workspace cache move failure. Existing system Java was not changed.
- Website is publicly deployed at https://pulse-status-zeta.vercel.app, with the dashboard at /app. AWS and Azure are source-link-only entries; Replicate's configured endpoint returned HTML and is explicitly unavailable. Official feeds are checked every two minutes while subscribed, with each completed result streamed to web/desktop clients. Readings expire after five minutes. Provider publication delays still apply; feed request duration is not provider API latency. Observed changes are held in memory (up to 250 events) and reset when the monitor restarts. The map uses reference hub coordinates with fresh status from explicitly named regional components; provider-wide issues appear separately without inferred geographic scope. Sector and workflow insights describe potential exposure, not verified downstream outages.















- Latest inbox/insight pass: 35 Node tests pass, including latest-update ordering and body selection, missing/invalid timestamp fallback, provider-scoped deduplication, stale/closed exclusions, unread revision tracking, and component/incident insight severity. Five native Java tests remain passing. Browser checks verified All services vs My watchlist, incident search, mark-as-read plus unread filtering, persistence across reload on a separate test origin, and current-check/source-update wording. Sidebar profile and top-right avatar are absent. Insights show specific evidence and possible effects, support pointer and keyboard watchlist filtering, and successfully copy a scoped summary. The dedicated insights page no longer repeats overview cards. Phone inbox and insights were checked at 390 px with no horizontal overflow.

- Map and notification design update: shared shape-and-color severity symbols, maintenance service filters, enlarged phone markers and displaced London/Frankfurt markers with location pointers. Browser fixture verified maintenance alongside outage on one provider, matched evidence only, keyboard regional selection, recovery clearing counts, and expiry clearing all nine pins. Light/dark desktop and 390 px phone map/inbox views checked; page and modal have no horizontal overflow. Inbox search empty state verified. Tests cover maintenance evidence isolation, incident-specific severity, and local date grouping; all 39 Node tests pass. Existing Android device-level notification and widget limitations still apply.
- Final package checks for this design: APK signature and 16 KB zip alignment passed; all 12 APK web/brand assets match current dist by SHA-256. All 12 Windows ASAR web/brand assets and four background modules match current sources. Packaged Windows HTML and status API smoke checks passed with 28 providers (13 fresh during that check; coverage is time-dependent). Five native Java tests remain passing.

- Mobile overview update: dedicated compact layout at 760 px and below, with a watchlist-first summary, severity-prioritized service rows, two relevant incident updates, progressive service search, and map/insights shortcuts. Full map/table/summary-grid are not mounted in the phone overview. Browser checks at 320 px and 390 px verified light/dark readability and no horizontal overflow; 1,440 px retains the desktop map, four summary cards and directory. Search opened npm details correctly, directory expanded from six to twelve rows, and map navigation reset scroll position to zero. Synthetic empty-watchlist, unavailable and operational states were checked separately from production. Reload plus an immediate desktop-to-phone resize was reproduced and fixed by synchronizing the media state when its listener subscribes. Three focused overview tests bring the Node suite to 42 passing tests.
- Mobile overview packages: Android rebuild passed with five native Java tests still passing; APK signature, alignment, and all 12 web/brand asset hashes verified. The rebuilt Windows ASAR also contains matching current web assets and unchanged background modules. Physical Android device behavior was not retested for this UI change.

- Contained map workspace: Global map uses the viewport below the app toolbar, with no separate header actions, footer, legend or service lists below the map. Desktop floating inspector and mobile bottom sheet contain service evidence, source timestamps, developer next steps and watch controls. Checked 1,440 x 1,000, 390 x 844, 320 x 568 and 740 x 360: document height equals viewport height, with no horizontal overflow; card content scrolls internally. Nearby London/Frankfurt touch targets were separated and London selection verified. Live regional selection, incident details, severity filter, watchlist scope (4/4 current feeds in the tested snapshot), Asia Pacific selection, zoom, reset and Escape dismissal passed. An isolated fixture verified watch removal/addition, maintenance-only evidence on a service with an outage, expiry clearing all visible source details and colored pins, and recovery returning three regional signals to operational. The 44 passing Node tests include deterministic bounded marker layout with source coordinates preserved. Export brief implementation/sidebar action and repeated page-header watch buttons are removed.
- Final contained-map package validation: APK signature/alignment passed, and all 10 current web/brand assets match dist by SHA-256 in both APK and Windows ASAR. The export-share chunks are no longer part of the web build. Four existing Windows background modules also match their sources; the native notification implementation is unchanged. Android build and its five existing Java unit tests passed; device-level notification delivery remains untested in this environment.

- Signal logo redesign: replaced the P monogram with opposing receiver arcs around a central node while preserving the wordmark, fonts and neutral themes. Checked the rendered sidebar in light/dark themes and the 16/24/32/48/64 px preview. Shared geometry generates the UI symbol, favicon, SVG/PNG brand assets, Android notification vector, five adaptive launcher densities and seven Windows icon sizes. Web build, Capacitor sync, Android and Windows portable builds passed. All seven current icon resources are verified inside both Windows executables; all 10 web/brand assets match dist by SHA-256 in APK and Windows ASAR. APK signature and alignment passed. Device-level launcher and notification rendering remain untested.

- Bottom Settings correction: Settings is now a labeled 44 px row at the bottom of the sidebar, below Help & methodology, replacing the former free-access footer position. The header gear is removed. Browser checks at 1440 x 900, 390 x 844 and 320 x 568 verified the row remains inside the viewport with 8 px bottom clearance and no horizontal overflow. Desktop and phone clicks open Your preferences; on phone the navigation drawer closes. Web build and Android sync/build passed; APK signature/alignment passed. All 10 current web/brand assets match dist in both APK and Windows ASAR, including the corrected sidebar. Physical Android interaction was not tested.
- Corrected-sidebar Windows portable EXE packaging completed successfully; release sizes and SHA-256 checksums refreshed for both downloads.

- Marketing site: built a separate web-first landing page with the existing fonts, actual 1803 x 1082 application screenshots, a light/dark preview switch, four keyboard-accessible tour tabs, an image dialog, FAQs and three platform cards. Generated Android art is saved in public/marketing/android-mascot.png; Windows/browser art uses CSS. Checked desktop at 1440 px and phones at 390/320 px. Tour selection, image open/close, theme preview, FAQ expansion and platform link targets passed. A decorative glow causing mobile overflow was corrected. All 47 Node tests pass, including hosted API response, error and method behavior. Native packages were rebuilt with public download links; APK signature/alignment and current JS hash passed, Windows assets match dist. Marketing assets are excluded from the native builds.

- Vercel production deployment: pulse-status-zeta.vercel.app is Ready on commit 68f0142. Verified HTTP 200 and distinct marketing/dashboard titles for / and /app. Initial platform routing exposed the dashboard at root; corrected by making marketing the physical web index and routing /app to dashboard.html. The public API returned all 28 providers with 25 fresh feeds at 2026-09-13T14:40:24Z. Followed Open Pulse into the live app and checked the device-download dialog URLs.

- Public downloads: GitHub release v1.0.0 is published. Anonymous HEAD requests return HTTP 200 for the APK (8,139,828 bytes), Windows EXE (103,239,713 bytes), and SHA256SUMS.txt. GitHub asset digests match the checksum manifest: APK 9406135C56D69B9BE94FFC7944AAA541ABF9884470D82085219C2B0D3D1F7909; public Windows build 096AF324C967533D38CF362D81E9DBEE44FBF6CADF36C63B77C6987E1A416392. The public Windows executable was built on GitHub Actions run 34764231046, where packaging, all 47 Node tests, and release upload passed. It is a separate build from the local EXE listed above. An initial CI run needed the explicit Electron runtime install step; the corrected manual workflow is checked in. The slow local Windows upload was canceled before publishing.
- Hosted mobile download entry: at 390 x 844, Get the app opens the installer dialog, the Settings bottom edge is 836 px, and the document has no horizontal overflow. Production homepage and dashboard routes are distinct and operational.

- Playwright screenshot quality update: replaced five JPEG captures with ten lossless PNGs captured from the live app at an exact 1440 x 900 viewport. Each view has a 1440 x 900 CSS-scale variant and a 2880 x 1800 device-scale variant, after waiting for live readings and fonts. Responsive source selection supplies enough pixels for desktop at 2x and a 390 px phone at 3x. Playwright verified theme and tour switching, full-resolution zoom, Android illustration resolution and absence of horizontal overflow. PNG dimensions and SHA-256 hashes are recorded in public/marketing/screenshots.json. Web production build passed; native installers are unaffected.

- Native UI update v1.0.1: Android and Windows now package the compact sidebar and smaller wordmark, removed Watchlist background card, simplified all-disruption/watchlist Dependency insights, search-first mobile Overview, and alert settings control beside Refresh. Android version code is 2; its signing certificate matches v1.0.0. Signature and 16 KB zip alignment passed, and all 19 packaged web assets match the current native build. Native Java test results have zero failures (five feed tests and one template test; unchanged tests were reused by Gradle).
- Public v1.0.1 release: https://github.com/haroontrailblazer/Pulse/releases/tag/v1.0.1. Android APK: 5,921,652 bytes, SHA-256 23FD1833C309F0537FB64519A9E806230F52A26A760266E46C58A08F4C24BADE. Windows EXE: 103,237,987 bytes, SHA-256 C3EF9B266AB34499BB09C1D50AE4AD49643FCFBD192D1CFF497D00F4210D4194. Both GitHub asset digests match the published checksum manifest. Windows CI run 34766893282 passed packaging, 48 Node tests, and comparison of all 10 web/brand assets and four background modules against current sources. A separate local Windows executable was also rebuilt. APK remains development-signed and Windows remains unsigned; physical Android notification delivery and widget behavior were not retested.

- Navigation release v1.0.2: removed the app top bar, placed the notification button beside each page title except Global map, moved the theme toggle to Settings, and reordered navigation to Overview, Incidents, Watchlist, Global map, Developer tools, Dependency insights. Playwright checked all six pages at 1440x900, 390x844, 320x568, and 740x360: title/bell alignment, inbox opening, menu navigation, no horizontal overflow, full-viewport map without a bell, and persisted theme changes all passed. Screenshot capture now opens Settings when changing themes.
- Android v1.0.2 (version code 3): signature and 16 KB alignment passed; signing certificate matches prior releases; all 19 packaged web assets match the native build. APK is 5,921,896 bytes with SHA-256 D334D0BEF795F0F59D8D5C4C30616C037E2DC5735B8BBCA562C664D33627EFEB. Windows run 34767742506 passed packaging, all 48 Node tests, and verification of 10 web/brand assets and four background modules. Windows EXE is 103,239,982 bytes with SHA-256 1B8C6612713A45906E37A3BE6C721A778585D17A6FBE723A7F3127963B684E5A. GitHub asset digests match the release checksum manifest. Native Java checks passed using unchanged cached tests; physical Android notifications and widget behavior were not retested. APK is development-signed and Windows remains unsigned.

- Direct installer CDN: website download buttons now use /downloads/v1.0.2 on pulse-status-zeta.vercel.app. The Vercel deployment fetches the release once and validates pinned byte counts and SHA-256 before staging static files; no user download passes through GitHub or a serverless proxy. Versioned files use immutable caching and attachment headers. Corruption, truncation, oversize responses, HTTP errors, and cache validation are covered by six passing mirror checks.
- Independent download verification run 34768615320 fetched every byte of both CDN installers, matched the published checksums, and verified HTTP 206 with correct Content-Range for resumed requests. APK: 5,921,896 bytes in 0.65 seconds; Windows: 103,239,982 bytes in 2.25 seconds from the GitHub-hosted test runner (not a promise of user-device speed). Local network checks remained slow on both GitHub and Vercel; the GitHub sample transferred 671,744 bytes before a 20-second timeout, and a CDN range request transferred 993,790 bytes in 25 seconds. The direct APK response showed a Vercel cache HIT in bom1, correct size, MIME type and attachment headers. Live browser checks confirm both landing-page platform buttons target the CDN.
