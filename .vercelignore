.cache
# Native source is included in the release fingerprint written into the web
# build, so these source directories must remain available during deployment.
releases
design
tests
node_modules
dist
.git
